import styled from 'styled-components';

// eslint-disable-next-line import/prefer-default-export
export const Container = styled.div`
  width: 93vw;
  height: 12vh;
  display: grid;
  grid-template-columns: 15vw auto 5vw 5vw;
  margin: 0 3vw 0 3vw;
  place-items: center;
`;
